#include <stdio.h>
#include <stdlib.h>
#include <string.h>
typedef struct{
    int id,idade;
    float renda;
    char nome [40];
}form;
int main(void){
    form form1;
    printf("\nFormulário: ");
    for(int i=0;i<5;i++){
        printf("\n Digite o id do %d aluno", i+1);
        scanf("%d",&form1[i].id);
        printf("\n Digite a idade do %d aluno", i+1);
        scanf("%d",&form1[i].idade);
        printf("\n Digite o nome do %d aluno", i+1);
        gets(&form1[i].nome);
        getchar();
    }
    for(int i=0;i<5;i++){
        if(form1[i].idade<18{
            maior++
        }
        printf("\n Temos %d alunos maiores de idade",maior++);
    }
    return(0);

}
