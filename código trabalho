#include <stdio.h>
#include <stdlib.h>
#include<string.h>
#include<conio.h>
typedef struct{
    int idade;
    char nome [40];
    float salario;
}funcionarios;
int main(void){
    funcionarios func[5];
    int n;
    float mediasalarial;
    for(int i=0;i<5;i++){
        printf("Digite a idade do %d° funcionário: \n",i+1);
        scanf("%d",&func[i].idade);
        getchar();
        printf("Digite o nome do %d° funcionário: \n ",i+1);
        fgets(func[i].nome,sizeof(func[i].nome),stdin);
        printf("Digite o salário do %d° funcionário: \n",i+1);
        scanf("%f",&func[i].salario);
    }
    
